[pytest]
markers =
    integration: tests that require a Whisper model download (deselect with '-m "not integration"')
    requires_subprocess: tests that legitimately spawn real subprocesses (opts out of conftest subprocess guard)
addopts = -m "not integration"
